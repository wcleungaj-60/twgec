alias addSolider(name, camp2, x, y) {
    addActor(name = name, camp = camp, hp = 100, x = x, y = y);
    addActor(name = name, camp = camp, hp = 100, x = x, y = y);
}

block myBlock {
    actions {
        addSolider(name = "天影士兵", camp = "天影", x = 1, y = 1);
    }
}