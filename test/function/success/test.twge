def addSolider(name, camp, x, y) : actions {
    addActor(name = name, camp = camp, hp = 100, x = x, y = y);
    addActor(name = name, camp = camp, hp = 101, x = x, y = y);
}

block myBlock {
    actions {
        addSolider(name = "天影" + "士兵", camp = "天影", x = 1, y = 1);
        addActor();
        addSolider(name = "天影士兵", camp = "天影", x = 2, y = 1);
        addSolider(name = "天影士兵", camp = "天影", x = 3, y = 1);
        addActor();
        addSolider(name = "天影士兵", camp = "天影", x = 4, y = 1);
    }
}