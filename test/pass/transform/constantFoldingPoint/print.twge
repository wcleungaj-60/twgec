//---------------------------------//
//--- AST After constantFolding ---//
//---------------------------------//
block init {
    actions {
        addActor(patrol = [Point(1,1),Point(2,2)]);
        addActor(patrol = [Point(2,2),Point(0,0),Point(2,2)]);
        addActor(patrol = [Point(2,1)]);
    }
    checks {
    }
    triggers {
    }
}
