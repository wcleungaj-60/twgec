//---------------------------------//
//--- AST After constantFolding ---//
//---------------------------------//
def testBool(v) : actions {
    actorTalk(cleanTalk = v);
}
block init {
    actions {
        testBool(false);
        testBool(true);
        testBool(true);
        testBool(false);
    }
}
