//---------------------------------//
//--- AST After constantFolding ---//
//---------------------------------//
block b3 {
    actions {
        for(x in [1,2,3,10,20,100]) {
            addActor(x = x);
        }
        for(x in ["abc","dce","fgh"]) {
            for(z in x) {
                addActor(name = z);
            }
        }
    }
}
//----------------------------------//
//--- AST After forLoopUnrolling ---//
//----------------------------------//
block b3 {
    actions {
        addActor(x = 1);
        addActor(x = 2);
        addActor(x = 3);
        addActor(x = 10);
        addActor(x = 20);
        addActor(x = 100);
        addActor(name = "a");
        addActor(name = "b");
        addActor(name = "c");
        addActor(name = "d");
        addActor(name = "c");
        addActor(name = "e");
        addActor(name = "f");
        addActor(name = "g");
        addActor(name = "h");
    }
}
