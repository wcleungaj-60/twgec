block init {
    actions {
        setCookie(playerId = "{instance.id}", cookies = "AGI", type = "integer", value = "{instance.AGI}");
        儲存Cookies(cookies = "{instance.id}.AGI", type = "integer", value = "{instance.AGI}");
    }
}